class Car {
    constructor(id, plate, model, categoryId) {
        this.id = id;
        this.plate = plate;
        this.model = model;
        this.categoryId = categoryId;
    }
}

export default Car;
